/*
 * The previous scripted assistant contained legacy pricing and sample-proof
 * copy. It is intentionally disabled for the refreshed public site: visitors
 * are routed to the service pages and a human contact path instead of being
 * given stale or unsupported answers.
 */
window.PkChatbot = { disabled: true };
